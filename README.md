# Hermes Jev Skills

An experimental, opt-in Hermes plugin for TypeSafe Jev-assisted skill discovery.

The first version is additive: the existing skill index and system prompt remain unchanged. Jev suggests candidates; the main agent decides what to load through normal `skill_view` calls.

Implementation and verification are in progress. No plugin is enabled in the active Hermes profile.
